# TestEnumZone
**.NET Console Application project** created to test things I can do with `C# Enum`

## Setup environment
Please install `.Net Core SDK 10.0` on Microsoft official Download page 👌
Use `Visual Studio 2022+` or `Visual Studio Code` with `C# Dev Kit extension` installed to open and start project.

## Build and run Project 
- **(recommended):** Use **Debug/Run** feature of `Visual Studio 2022+` or `Visual Studio Code`
- Commandline:
```sh
dotnet run
```

## Contribution
You can checkout a branch, then Pull Request to `master`!
```sh
git checkout -b whatever-branch-name
```
When you **push code** to this repository or create **Pull request** to `master` branch, **Automated CI/CD Flow will run to check build & Unit test result.**
I will **check your code and CI/CD result** to decide to approve or not.
* **Please note that:** Only me can merge request to `master` branch as I protected it.

### Thank you for reading.
